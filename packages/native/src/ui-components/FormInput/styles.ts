import {StyleSheet} from 'react-native'

import colors from '../../theme/colors'

export default StyleSheet.create({
  errorText: {
    color: colors.nfl.secondary,
    fontWeight: 'bold',
    fontSize: 14,
    marginBottom: -15
  },
  container: {
    marginTop: 16
  }
})
